import { CurrentFormulaDirective } from './current-formula.directive';

describe('CurrentFormulaDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentFormulaDirective();
    expect(directive).toBeTruthy();
  });
});
