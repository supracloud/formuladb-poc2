{
  "name": "formuladb",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "rm -rf dist && tsc --inlineSourceMap -p src",
    "start": "node dist/be/src/server.js",
    "initpgdb": "node storage/bin/start_postgres.js",
    "db": "(docker rm -f formuladb-pg || true) && docker-compose up -d db"
  },
  "dependencies": {
    "circular-json": "^0.5.9"
  },
  "devDependencies": {}
}
