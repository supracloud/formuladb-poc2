/**
 * * first is the CSS layer, we should allow as much as possible customization at the CSS level. Here the color palette will be customizable
 * * second layer is the template layer, we should allow developers to rewrite some of the angular templates at their own risk: 
 *   - they must use all bindings and transclusion that is present in the default theme unless they want to loose functionality or completely break the application
 */   