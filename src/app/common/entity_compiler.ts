import { Entity } from "./domain/metadata/entity";

/**
 * The compiler must produce execution plans for entities
 */
export class EntityCompiler {
    public compileEntity(entity: Entity) {
        
    }
}