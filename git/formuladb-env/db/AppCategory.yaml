_id: AppCategory
isEditable: true
props:
    _id:
        name: _id
        propType_: STRING
        allowNull: false
    guiorder:
        name: guiorder
        propType_: STRING
        allowNull: false
    _owner: { name: _owner, propType_: STRING, allowNull: false }
    _role: { name: _role, propType_: STRING, allowNull: false }
    _rev: { name: _rev, propType_: STRING, allowNull: false }
