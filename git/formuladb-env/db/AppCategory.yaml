_id: AppCategory
isEditable: true
props:
    _id:
        name: _id
        propType_: STRING
        allowNull: false
    guiorder:
        name: guiorder
        propType_: STRING
        allowNull: false
